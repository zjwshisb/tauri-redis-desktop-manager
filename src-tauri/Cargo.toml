[package]
name = "tauriredis"
version = "0.0.0"
description = "A Tauri App"
authors = ["you"]
license = ""
repository = ""
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[build-dependencies]
tauri-build = { version = "1.2", features = [] }

[dependencies]
tauri = { version = "1.2", features = ["dialog-all", "fs-all", "shell-open", "window-all"] }
tokio-stream = "*"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
redis = { version = "0.23.1", features = ["tokio-comp", "cluster-async"], git="https://github.com/redis-rs/redis-rs.git" }
rusqlite = { version = "0.29.0", features = ["bundled"] }
dirs-next = "2.0.0"
thiserror = "1.0.40"
tokio = { version = "1", features = ["full"] }
once_cell= "*"
futures = "*"
rand="*"
chrono="*"
hex="*"

[features]
# this feature is used for production builds or when `devPath` points to the filesystem
# DO NOT REMOVE!!
custom-protocol = ["tauri/custom-protocol"]
